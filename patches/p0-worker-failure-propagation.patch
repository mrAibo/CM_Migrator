diff --git a/src/com/ibm/ecm/migration/Producer.java b/src/com/ibm/ecm/migration/Producer.java
--- a/src/com/ibm/ecm/migration/Producer.java
+++ b/src/com/ibm/ecm/migration/Producer.java
@@
     private final MigrationJournal journal;
     private final MigrationStats stats;
+    private final WorkerFailureState workerFailures;
     private final int consumerCount;
@@
-    public Producer(BlockingQueue<MigrationItem> queue, MigrationConfig config, MigrationJournal journal, MigrationStats stats) {
+    public Producer(BlockingQueue<MigrationItem> queue,
+                    MigrationConfig config,
+                    MigrationJournal journal,
+                    MigrationStats stats,
+                    WorkerFailureState workerFailures) {
         this.queue = queue;
         this.config = config;
         this.journal = journal;
         this.stats = stats;
+        this.workerFailures = workerFailures;
         this.consumerCount = config.getThreadCount();
     }
@@
                 if (!VALID_ITEM_TYPE.matcher(sourceType).matches() || !VALID_ITEM_TYPE.matcher(destType).matches()) {
-                    logger.error("SECURITY ALERT: Invalid ItemType format detected! Source={}, Dest={}. Skipping.", sourceType, destType);
-                    continue;
+                    IllegalArgumentException failure = new IllegalArgumentException(
+                            "Invalid ItemType format: " + sourceType + " -> " + destType);
+                    workerFailures.record(failure);
+                    logger.error("SECURITY ALERT: Invalid ItemType format detected! Source={}, Dest={}",
+                            sourceType, destType, failure);
+                    ShutdownCoordinator.requestShutdown();
+                    break;
                 }
@@
                     } catch (Exception e) {
+                        workerFailures.record(e);
                         logger.error("Error processing ItemType {}", sourceType, e);
+                        ShutdownCoordinator.requestShutdown();
                     } finally {
                         ThreadContext.clearAll();
                     }
@@
-            } else {
+            } else if (workerFailures.hasFailure()) {
+                logger.error("Producer stopping after asynchronous worker failure. Skipping Poison Pills.");
+            } else {
                 logger.info("All ItemTypes processed. Enqueuing Poison Pills...");
                 enqueuePoisonPill();
             }
@@
         } catch (Exception e) {
             logger.error("Error in Producer for type " + sourceType, e);
+            throw new IllegalStateException("Producer failed for ItemType " + sourceType, e);
         }
     }
diff --git a/src/com/ibm/ecm/migration/Main.java b/src/com/ibm/ecm/migration/Main.java
--- a/src/com/ibm/ecm/migration/Main.java
+++ b/src/com/ibm/ecm/migration/Main.java
@@
         // 1. Load Config
         MigrationConfig config = new MigrationConfig(configPath);
+        WorkerFailureState workerFailures = new WorkerFailureState();
@@
-        Producer producer = new Producer(queue, config, journal, stats);
+        Producer producer = new Producer(queue, config, journal, stats, workerFailures);
         workerExecutor.submit(producer);
@@
         if (ShutdownCoordinator.isShuttingDown()) {
             aborted = true;
         }
+
+        Throwable workerFailure = workerFailures.get();
+        if (workerFailure != null) {
+            logger.error("Migration worker failed", workerFailure);
+            aborted = true;
+        }
@@
         System.out.println(ConsoleUI.separator());
+
+        if (workerFailure != null) {
+            throw new IllegalStateException("Migration failed in asynchronous worker", workerFailure);
+        }
     }
